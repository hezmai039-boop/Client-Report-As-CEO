# شيت UI/UX الشامل — منصة مساري لتقارير العملاء
## Design System & Handoff Specs — للتطبيق المباشر في Base44

**علاقة هذه الوثيقة بما سبق:** هذه هي **الطبقة البصرية** لأمر الإطلاق التجاري (صفحة الهبوط + مسار الاشتراك). طبِّقها على كل الصفحات العامة (الهبوط، `/subscribe`، `/e/:token`، الخصوصية، الشروط) وعلى لوحة الإدارة حيث ينطبق. **لا تلمس أي منطق خلفي أو كيانات أو دوال — تصميم فقط.**

**قاعدة اللون الحاكمة:** هوية مساري **زرقاء داكنة `#1F3A5F`** حصراً. أي بنفسجي/وردي في أي مرجع بصري يُستبدل بمقابله من سلم الأزرق أدناه.

---

## 1. الألوان (Design Tokens)

```css
:root {
  /* الأساسي — سلم الأزرق المشتق من هوية مساري */
  --primary-900: #16293F;  /* خلفيات داكنة عميقة (تذييل، CTA النهائي) */
  --primary-800: #1F3A5F;  /* اللون الأم — عناوين، أزرار أساسية، شريط جانبي */
  --primary-600: #2E5288;  /* تدرجات، حالات hover للأزرار الأساسية */
  --primary-400: #5B7FAE;  /* عناصر ثانوية، حدود مميزة */
  --primary-100: #E8EEF6;  /* خلفيات أيقونات الميزات، خلفيات فاتحة مميزة */
  --primary-50:  #F4F7FB;  /* خلفيات أقسام متناوبة */

  /* التمييز (Accent) — للعدادات، الشارات، النجاح */
  --accent-500: #2BB673;   /* أخضر نجاح/نمو */
  --accent-amber: #F0A93B; /* تنبيه/نجوم التقييم */

  /* المحايد */
  --ink-900: #1A2332;      /* نص العناوين */
  --ink-600: #4A5568;      /* نص الجسم */
  --ink-400: #8896A6;      /* نص ثانوي/تلميحات */
  --surface: #FFFFFF;
  --bg-page: #FAFBFD;
  --border: #E4E9F0;

  /* التدرج الرئيسي (Hero وCTA) */
  --gradient-hero: linear-gradient(135deg, #16293F 0%, #1F3A5F 55%, #2E5288 100%);
}
```

**قواعد الاستخدام:**
- زر أساسي: خلفية `--primary-800`، نص أبيض، hover إلى `--primary-600`
- زر ثانوي: حد `--primary-800` بسماكة 1.5px، نص `--primary-800`، خلفية شفافة، hover خلفية `--primary-50`
- لا يُستخدم الأخضر إلا للدلالة (نجاح، نمو ▲، حالة نشط) — ليس لوناً زخرفياً
- الأقسام تتناوب: أبيض ← `--primary-50` ← أبيض (إيقاع بصري يفصل الأقسام بلا خطوط)

## 2. الطبوغرافيا

**الخط:** `IBM Plex Sans Arabic` (Variable) أساسياً، و`Cairo` احتياطياً:
`font-family: "IBM Plex Sans Arabic", "Cairo", "Segoe UI", Tahoma, sans-serif;`

| العنصر | الوزن | الحجم Desktop | الحجم Mobile | Line-Height |
|---|---|---|---|---|
| Hero H1 | 800 | 44px | 30px | 1.25 |
| H2 (عناوين الأقسام) | 700 | 32px | 24px | 1.35 |
| H3 (عناوين الكروت) | 700 | 19px | 17px | 1.4 |
| Body | 400 | 16px | 15px | 1.8 |
| Body ثانوي | 400 | 14px | 13.5px | 1.7 |
| شارات/Badges | 600 | 13px | 12px | 1 + letter-spacing 0.5px |
| أرقام العدادات | 800 | 40px | 30px | 1 |

- **الأرقام في الإحصاءات والأسعار لاتينية** (199 لا ١٩٩) لوضوح أعلى — النص المحيط عربي
- عنوان الـHero يحوي **كلمة واحدة مميزة** بلون `--primary-400` أو بخط تحتي متدرج (مثال: «بعين **مدير تنفيذي**»)

## 3. الشبكة والتخطيط

- **Max-width:** `1200px` للمحتوى، توسيط تلقائي، padding جانبي `24px` (موبايل: `16px`)
- **إيقاع رأسي:** كل قسم `padding: 96px 0` (موبايل: `56px 0`)
- **الشبكات:** الميزات 3 أعمدة ← 2 (تابلت) ← 1 (موبايل)؛ الباقات 3 ← 1؛ الشهادات 3 ← سحب أفقي snap على الموبايل
- **Border Radius:** كروت `20px` · أزرار `12px` (زر الـHero الأساسي Pill كامل `999px`) · حقول `12px` · صور الأجهزة `24px`
- **الظلال:**
  - كرت ساكن: `0 10px 30px rgba(22,41,63,0.05)`
  - كرت hover: `0 18px 40px rgba(22,41,63,0.10)`
  - الباقة المميزة: `0 20px 50px rgba(31,58,95,0.15)` + حد علوي `4px` بلون `--primary-800`

## 4. الحركات (Animations & Micro-interactions)

```css
/* ظهور عند التمرير — يطبق على كل قسم وكروته */
.fade-up { opacity: 0; transform: translateY(24px);
  transition: all .6s cubic-bezier(0.16, 1, 0.3, 1); }
.fade-up.visible { opacity: 1; transform: translateY(0); }
/* تتابع الكروت داخل الشبكة الواحدة */
.stagger > *:nth-child(1){transition-delay:0s} .stagger > *:nth-child(2){transition-delay:.1s}
.stagger > *:nth-child(3){transition-delay:.2s} .stagger > *:nth-child(4){transition-delay:.3s}
```

| العنصر | التفاعل |
|---|---|
| كروت الميزات/الباقات | hover: `translateY(-6px)` + الظل الأعمق، transition `.3s ease` |
| الأزرار | hover: `scale(1.02)` + تفتيح الخلفية درجة؛ active: `scale(0.98)` |
| صورة الجهاز في الـHero | ميلان ثابت `rotate(-4deg)` + تحويم خفيف مستمر (`translateY ±8px` بمدة 6s ease-in-out infinite) |
| العدادات الرقمية | Count-up من 0 عند دخول القسم للشاشة، مدة 1.5s |
| شعارات العملاء | Grayscale افتراضياً ← ألوانها عند hover، transition `.3s` |
| أكورديون الأسئلة | أيقونة `+` تدور `45deg` عند الفتح؛ ارتفاع المحتوى بـ `.35s ease` |
| شريط تقدم نموذج العميل | تعبئة بحركة `.4s ease` عند كل خطوة |

**قواعد إلزامية:**
- احترام `prefers-reduced-motion: reduce` — تعطيل كل الحركات لمن طلب ذلك
- الحركة تعمل **مرة واحدة** عند أول ظهور (لا تتكرر مع كل تمرير)
- لا حركة تتجاوز `0.6s` — السرعة إحساس بالجودة

## 5. هيكل صفحة الهبوط قسماً-قسماً (محاكاة بنية المرجع بهوية مساري)

**١. شريط تنقل ثابت (Sticky):** شفاف فوق الـHero يتحول لأبيض بظل خفيف عند التمرير. شعار مساري يميناً، روابط (المميزات، الباقات، الأسئلة)، وزر «اشترك الآن» أساسياً يساراً.

**٢. Hero:** خلفية `--gradient-hero` مع أشكال SVG هندسية ناعمة شفافة (دوائر/منحنيات بشفافية 5-8%). النص يميناً (العنوان + الفرعي + زرّان: «اشترك الآن» Pill أبيض بنص أزرق + «شاهد نموذج تقرير» حدودي أبيض شفاف)، وصورة الجهاز يساراً مائلة `-4deg` تعرض تقرير مساري الفعلي داخل إطار هاتف، بتحويم مستمر ناعم. أسفل الـHero موجة SVG بيضاء تفصل عن القسم التالي.

**٣. شريط ثقة مصغّر:** مباشرة تحت الـHero — 4 عدادات (Count-up): «+X تقرير أُنجز» · «X قطاعات مخدومة» · «دقة إدخال 100%» · «متابعة 24/7». خلفية بيضاء، أرقام `--primary-800` كبيرة.

**٤. المشكلة ← الحل (سطران + عنوان):** خلفية `--primary-50`.

**٥. كيف تعمل — 3 خطوات:** كروت بيضاء radius `20px`، كل كرت: أيقونة SVG داخل دائرة `64px` بخلفية `--primary-100`، رقم الخطوة كشارة صغيرة، عنوان H3 وسطر وصف. ظهور متتابع stagger.

**٦. شبكة الميزات (6 كروت):** تقارير آلية ذكية · استبيان على مقاسك · مقارنات ومؤشرات · خصوصية تامة · بلا جداول ولا تعقيد · دعم مساري المباشر. نفس نمط الكروت مع hover الارتفاع.

**٧. معاينة تقرير حقيقي:** عنوان «هكذا يصلك تقريرك» + التقرير الفعلي (قالب HTML الحقيقي ببيانات وهمية واقعية لمطعم) داخل إطار متصفح/جوال بظل عميق، مائل بزاوية خفيفة، مع نقطتي شرح جانبيتين (سهم لـ«مقارنة بالفترة السابقة» وسهم لـ«توصيات مخصصة لك»).

**٨. لمن المنصة (4 قطاعات):** كروت أفقية صغيرة بأيقونة + سطر واحد لكل قطاع.

**٩. آراء العملاء:** 3 كروت، كل كرت: 5 نجوم بلون `--accent-amber`، نص الشهادة، ثم اسم عام («صاحب مطعم — الرياض») بلا أسماء حقيقية. على الموبايل: سحب أفقي بـ scroll-snap.

**١٠. الباقات:** 3 كروت، الوسطى مرفوعة `translateY(-12px)` بشارة «الأكثر اختياراً» وحد علوي أزرق. كل باقة: الاسم، السعر بخط 800 كبير + «ر.س/شهرياً» صغيرة، قائمة مميزات بأيقونات ✓ خضراء، زر «اشترك الآن» (أساسي للوسطى، ثانوي للطرفيتين). الأسعار من الإعدادات لا ثابتة بالكود.

**١١. الأسئلة الشائعة:** أكورديون بحد `--border` وradius `16px` لكل سؤال، أيقونة `+` تدور عند الفتح.

**١٢. CTA نهائي:** شريط بعرض كامل بخلفية `--gradient-hero`، عنوان «جاهز ترى أرقامك بعين جديدة؟» + زر أبيض Pill كبير «اشترك الآن»، مع نفس الأشكال الهندسية الناعمة.

**١٣. تذييل:** خلفية `--primary-900`، 3 أعمدة (عن مساري، روابط سريعة، تواصل)، ثم سطر حقوق + روابط الخصوصية والشروط. نص بشفافية 70%.

## 6. مكوّنات النظام (تسري على كل الصفحات)

| المكوّن | المواصفة |
|---|---|
| حقل إدخال | ارتفاع `48px`، حد `--border`، radius `12px`؛ focus: حد `--primary-400` + ظل `0 0 0 3px var(--primary-100)` |
| رسالة خطأ حقل | نص أحمر `13px` تحت الحقل + اهتزاز خفيف `shake .3s` |
| Toast | أعلى يسار (RTL)، radius `12px`، شريط جانبي ملوّن بحسب النوع، اختفاء تلقائي 4s |
| شارة حالة | Pill بخلفية فاتحة ونص داكن من نفس العائلة (نشط: أخضر فاتح/داكن، معلَّق: كهرماني، فشل: أحمر) |
| جدول الإدارة | رأس `--primary-50`، صفوف تفصلها حدود `--border`، hover صف `--bg-page`، خلايا `padding 12px 16px` |
| Skeleton تحميل | بدل «جارٍ التحميل...» النصية — مستطيلات نابضة بلون `--primary-50` |
| صفحة فارغة (Empty State) | أيقونة كبيرة باهتة + جملة توجيهية + زر الإجراء الأول |

## 7. الأصول المطلوبة (Assets)

| الأصل | الصيغة | ملاحظات |
|---|---|---|
| `hero-device-mockup` | WebP @2x | تقرير مساري الفعلي داخل إطار هاتف — يُبنى بـ HTML/CSS داخل إطار جهاز مرسوم CSS إن تعذّر توليد صورة (أفضل: يبقى HTML حقيقياً فيظل حاداً على كل الشاشات) |
| حزمة أيقونات الميزات | SVG (Lucide المتوفرة في المشروع) | داخل دوائر `--primary-100`، بسماكة خط موحدة 1.5-2px |
| أشكال خلفية الـHero | SVG inline | دوائر ومنحنيات بشفافية 5-8%، لا صور نقطية |
| موجة فاصل الأقسام | SVG inline | فاصل الـHero والـCTA النهائي |
| شعارات عملاء | — | **مرحلة لاحقة**: لا تستخدم شعارات وهمية لشركات حقيقية إطلاقاً؛ حالياً استبدل قسم الشعارات بشريط العدادات (٣) |

## 8. الاستجابة وقواعد RTL

- **Mobile-first** — نقاط التحول: `640px` / `1024px`
- كل الصفحة `dir="rtl"`؛ الأيقونات الاتجاهية (أسهم) تنعكس تلقائياً؛ الأرقام والأسعار تبقى LTR داخلياً
- أهداف اللمس ≥ `44px`؛ لا hover-only معلومات على اللمس — كل تفاعل له مسار لمسي
- صورة الـHero تنتقل **تحت** النص على الموبايل بحجم مصغّر
- منع أي تمرير أفقي للصفحة نهائياً — الجداول والشهادات تتمرر داخل حاوياتها

## 9. إتاحة (Accessibility) — إلزامي

- تباين نص/خلفية ≥ 4.5:1 (كل التوكنز أعلاه محسوبة لتحقيقه — لا تفتيح نصوص فوق الأبيض دون فحص)
- كل الصور والأيقونات الدلالية بـ `aria-label` عربي؛ الزخرفية `aria-hidden`
- الأكورديون والتنقل قابلان للتشغيل الكامل بلوحة المفاتيح مع حالات focus ظاهرة (حلقة `--primary-400`)
- `prefers-reduced-motion` محترم كما في قسم الحركات

## 10. تعليمات التنفيذ لـ Base44

1. طبِّق هذا النظام على: صفحة الهبوط الجديدة، `/subscribe`، صفحة العميل `/e/:token` (حدّث ألوانها وحركاتها لتطابق التوكنز — بنيتها خطوة-بخطوة تبقى كما هي)، صفحتي الخصوصية والشروط، ولوحة الإدارة (توكنز وكروت وجداول فقط — بلا حركات تسويقية داخلها).
2. عرّف التوكنز مرة واحدة (Tailwind config أو CSS variables) — **ممنوع تكرار قيم الألوان يدوياً في الملفات**.
3. حمّل الخط بـ `font-display: swap` مع أوزان 400/600/700/800 فقط.
4. الحركات بـ IntersectionObserver — لا مكتبات حركة خارجية ثقيلة.
5. **لا تلمس:** الدوال الخلفية، الكيانات، RLS، قفل الاعتماد، منطق أي صفحة قائمة.
