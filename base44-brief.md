# أمر بناء شامل — منصة تقارير العملاء الآلية (مساري)

انسخ هذا المستند كاملاً والصقه كأمر بناء أولي في Base44.

---

## السياق

أنا أدير شركة استشارات باسم **"مساري لريادة الأعمال"**، وأقدّم لعملائي تقارير أداء دورية (يومية/أسبوعية/شهرية) مبنية على بيانات يدخلونها بأنفسهم. أحتاج منصة ويب كاملة تدير هذه العملية آلياً من الاستقبال حتى الإرسال، قابلة للتوسع لمئات العملاء مستقبلاً.

## المستخدمون

1. **أنا (المدير)** — أرى كل العملاء، أضيف عملاء جدداً، أولّد تقارير يدوياً عند الحاجة، أعبّئ استبيان حوكمة إدارية يومي لتوثيق عملي.
2. **العميل** — يدخل بيانات أدائه اليومية عبر رابط خاص به (لا يرى اسم أي أداة خارجية في الواجهة، فقط هوية "مساري")، ويستلم تقاريره بالبريد.

## البيانات الأساسية (الكيانات)

### عميل (Client)
- الاسم، البريد، القطاع، الاسم الظاهر
- حالة "نشط" تتحوّل تلقائياً إلى نشط بمجرد أول إدخال بيانات
- رابط فريد وعشوائي لإدخال بياناته (لا يحتاج تسجيل دخول ولا كتابة اسمه — الرابط نفسه يحدّده)
- أسئلة مخصّصة اختيارية إضافية يحدّدها المدير لكل عميل حسب طبيعة نشاطه

### إدخال يومي (Daily Entry)
حقول ثابتة: تاريخ البيانات، إجمالي الإيرادات، عدد العمليات، تكلفة البضاعة/التشغيل، عملاء جدد، عملاء متكررون، مصروفات التسويق، رضا العملاء (1-5)، أبرز صنف/خدمة، مؤشر قطاعي إضافي، ملاحظات اليوم — بالإضافة لإجابات الأسئلة المخصصة إن وُجدت.

**قيد صارم:** لا يمكن إطلاقاً وجود أكثر من إدخال واحد لنفس العميل في نفس التاريخ (قيد فريد Unique على مستوى قاعدة البيانات، لا فحص برمجي فقط).

### تقرير (Report)
نوع الفترة (يومي/أسبوعي/شهري)، تسمية الفترة، الحالة (قيد الانتظار/أُرسل/محجوب/فشل)، ملخص وتوصيات مولَّدة بالذكاء الاصطناعي.

**قيد صارم مطابق للإدخال اليومي:** لا يمكن وجود أكثر من تقرير واحد لنفس العميل لنفس الفترة (قيد فريد على مستوى القاعدة) — لمنع إرسال نفس التقرير للعميل مرتين.

### سجل حوكمة إدارية يومي (Governance Entry) — لي أنا فقط
استبيان يومي من 6 أقسام: عمليات العملاء، المبيعات والنمو، المالية، الفريق والتشغيل، الحوكمة والقرارات، التخطيط. كل يوم صف واحد فقط، بطابع زمني تلقائي **لا يمكن تعديله رجعياً** بعد الحفظ — هذا هو الدليل على انتظام إدارتي أمام الملاك.

## ⚠️ القيد الأهم في المشروع كله — قفل الاعتماد المركزي

يجب أن يوجد إعداد عام واحد اسمه **`EXTERNAL_SEND_APPROVED`**، قيمته الافتراضية **`false`** حتماً.

- ما دامت `false`: **أي بريد كان سيصل عميلاً حقيقياً يجب أن يتحول تلقائياً إلى بريدي أنا (بريد المالك)** بعنوان يبدأ بـ "[معلَّق بانتظار الاعتماد]"، ويحتوي على اسم المستلم الأصلي المقصود داخل نص الرسالة — لأراجعه قبل أي إرسال فعلي.
- البريد الداخلي (لي أنا فقط — تنبيهات النظام، تقارير الحوكمة) **يمر دائماً بلا قفل**.
- لا تجعل هذا الإعداد قابلاً للتغيير من واجهة المستخدم العادية — فقط من إعدادات النظام/متغيرات البيئة، وبقيمة ابتدائية `false` غير قابلة للتفاوض.

هذا القيد أهم من أي ميزة أخرى في المشروع. لا تُنشئ أي مسار يرسل بريداً فعلياً لعميل يتجاوز هذا القفل.

## متطلبات الواجهة

1. **لوحة إدارة محمية بكلمة مرور** تعرض جدول العملاء (الاسم، الحالة، آخر بيانات، آخر تقرير، رابط الإدخال الخاص به)، وزر لتوليد تقرير فوري لأي عميل.
2. **صفحة إدخال بيانات عامة** لكل عميل عبر رابطه الفريد — **بلا أي ذكر أو شعار لأي جهة تقنية خارجية**، فقط هوية "مساري" (استخدم لوناً أزرق داكناً `#1F3A5F` كلون أساسي). حقل واحد ظاهر في كل مرة مع زر "التالي" (تجربة سلسة على الجوال)، وكل الحقول اختيارية عدا التاريخ.
3. **استبيان الحوكمة اليومي** بنفس الفلسفة — محمي، بطابع زمني ثابت.
4. **تقرير مُرسَل بالبريد** يحوي: أرقام الفترة، فقرة توصيات مولَّدة بالذكاء الاصطناعي (اربط بواجهة Gemini أو أي نموذج لغوي متاح لديك)، ومرفق PDF لنفس المحتوى.

## متطلبات التوسّع (مهم جداً)

يجب أن يعمل النظام لآلاف العملاء دون أي قيد بنيوي يرتبط بعددهم:
- لا تُنشئ أي مورد فريد لكل عميل عدا سجله في قاعدة البيانات ورابطه (لا Webhook منفصل، لا Cron Job منفصل لكل عميل).
- توليد التقارير الدورية يجب أن يمر عبر **طابور معالجة (Job Queue)** يعالج العملاء بالتوازي بدفعات محدودة مع إعادة محاولة تلقائية عند الفشل — لا حلقة تسلسلية واحدة قد تتعطل في المنتصف.
- التقارير اليومية تُجدوَل 7 صباحاً، الأسبوعية كل أحد، الشهرية أول كل شهر (بتوقيت الخادم).

## متطلبات إضافية

- استيراد بيانات تاريخية من ملفات CSV لكل عميل.
- اختبارات آلية للسلوكيات الحرجة الثلاثة: (1) قفل الاعتماد يعمل فعلاً، (2) المصادقة تحجب الوصول غير المخوَّل، (3) حساب الفترات الزمنية صحيح.

## المخرج المطلوب

مشروع كامل قابل للتشغيل محلياً وللنشر، مع ملف تعليمات تشغيل واضح، ومزامنة الكود إلى مستودع GitHub: **`hezmai039-boop/Client-Report-As-CEO`** (إن كانت لديك القدرة على الربط المباشر بـ GitHub؛ وإلا، جهّز حزمة تصدير كاملة لأرفعها يدوياً).

**لا تُفعِّل أي إرسال بريد فعلي لأي عميل تجريبي أثناء البناء أو الاختبار** — التزم بقفل الاعتماد الموضَّح أعلاه من أول سطر كود تكتبه.
